---
phase: 03-blog-core-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - backend/internal/modules/blog/repository.go
  - backend/internal/modules/blog/service.go
  - backend/internal/modules/blog/handler.go
  - backend/internal/modules/blog/dto.go
autonomous: true
requirements: [BLOG-04]
---

<objective>
Implement hierarchical post structure to support parent/child relationships.

Purpose: Allow the creation of "pillar" posts with associated "cluster" posts for better content organization and SEO.
Output: Parent/Child relationship logic and retrieval APIs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-blog-core-lifecycle/03-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hierarchy Storage</name>
  <files>backend/internal/modules/blog/repository.go</files>
  <action>
    Implement parent/child relationship storage (per D-04):
    - Add `parent_id` (UUID) to the Blog Post's JSONB data.
    - Create a GORM query in the repository to fetch all children of a given post ID.
    - Create a GORM query to fetch the parent of a given post.
    - Ensure an index is added to the `parent_id` within the JSONB for performance.
  </action>
  <verify>
    <automated>
      # Verify GORM query returns children for a given parent ID
      # (Agent should write a small test file or use internal tool)
    </automated>
  </verify>
  <done>Parent/Child relationships are persisted and queryable in DB</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hierarchical Retrieval Service</name>
  <files>backend/internal/modules/blog/service.go</files>
  <action>
    Implement service logic to handle the hierarchy:
    - `GetPostWithChildren`: Fetches a post and its direct children.
    - `GetPostBreadcrumbs`: Recursively fetches the path from the current post up to the root parent.
    - Prevent circular references (a post cannot be its own parent or a child of its own descendant).
  </action>
  <verify>
    <automated>
      # Test breadcrumb retrieval for a nested post
    </automated>
  </verify>
  <done>Hierarchical data can be retrieved as breadcrumbs or child lists</done>
</task>

<task type="auto">
  <name>Task 3: Implement Hierarchy Management API</name>
  <files>
    - backend/internal/modules/blog/handler.go
    - backend/internal/modules/blog/dto.go
  </action>
  <action>
    Create API endpoints for managing the hierarchy:
    - PATCH `/api/v3/blog/posts/:slug/parent` : Set or change the parent of a post.
    - GET `/api/v3/blog/posts/:slug/children` : List child posts.
  </action>
  <verify>
    <automated>
      curl -X PATCH http://localhost:8080/api/v3/blog/posts/child-post/parent -d '{"parent_id": "..."}' && \
      curl -X GET http://localhost:8080/api/v3/blog/posts/parent-post/children
    </automated>
  </verify>
  <done>Admin can organize posts into a hierarchy via API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> API | Request to change post hierarchy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | /api/v3/blog/posts/.../parent | mitigate | Enforce cycle detection to prevent infinite loops in breadcrumbs |
</threat_model>

<verification>
- A post can be assigned a parent.
- Circular dependencies are rejected.
- Breadcrumbs can be generated for deep hierarchies.
</verification>

<success_criteria>
- Hierarchical navigation is supported via API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-blog-core-lifecycle/03-03-SUMMARY.md`
</output>
