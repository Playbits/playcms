---
phase: 03-blog-core-lifecycle
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - backend/internal/modules/blog/repository.go
  - backend/internal/modules/blog/service.go
  - backend/internal/modules/blog/handler.go
  - backend/internal/modules/blog/dto.go
autonomous: true
requirements: [BLOG-05, BLOG-06]
---

<objective>
Implement full-text search using pg_trgm and a related posts recommendation engine.

Purpose: Enhance content discoverability for end-users.
Output: Search API and "Related Posts" logic.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-blog-core-lifecycle/03-03-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup pg_trgm and Search Index</name>
  <files>backend/internal/modules/blog/repository.go</files>
  <action>
    Implement the search infrastructure (per D-05):
    1. Create a migration or initialization step to enable `CREATE EXTENSION IF NOT EXISTS pg_trgm;`.
    2. Create a GIN index on the `title` and `content` (JSONB) fields of the posts using `gin_trgm_ops`.
    3. Implement a repository method that uses the `%` operator or `similarity()` function to perform fuzzy keyword search.
  </action>
  <verify>
    <automated>
      # Verify index exists in PostgreSQL
      psql -c "\d posts" | grep "gin_trgm_ops"
    </automated>
  </verify>
  <done>PostgreSQL is configured for high-performance fuzzy search on blog posts</done>
</task>

<task type="auto">
  <name>Task 2: Implement Search Service and API</name>
  <files>
    - backend/internal/modules/blog/service.go
    - backend/internal/modules/blog/handler.go
  </action>
  <action>
    Implement the search functionality:
    1. Service: Implement `SearchPosts(query string, limit, offset)` that calls the repository.
    2. Handler: Create GET `/api/v3/blog/search?q=...` endpoint.
    3. Ensure results are sorted by similarity score.
  </action>
  <verify>
    <automated>
      curl -X GET "http://localhost:8080/api/v3/blog/search?q=PlayCMS" | grep "test-post"
    </automated>
  </verify>
  <done>Users can find posts via fuzzy keyword search</done>
</task>

<task type="auto">
  <name>Task 3: Implement Related Posts Engine</name>
  <files>
    - backend/internal/modules/blog/service.go
    - backend/internal/modules/blog/repository.go
  </action>
  <action>
    Implement the recommendation logic (BLOG-06):
    1. Repository: Implement a query that finds posts sharing the most categories and tags with a given post ID, excluding the post itself.
    2. Service: Implement `GetRelatedPosts(postID UUID, limit int)`.
    3. Integrate into the `GetPostBySlug` response as a `related_posts` list.
  </action>
  <verify>
    <automated>
      # Create two posts with the same tag, then fetch one and check if the other is related
      curl -X GET http://localhost:8080/api/v3/blog/posts/test-post | grep "related_posts"
    </automated>
  </verify>
  <done>Related posts are automatically suggested based on shared taxonomy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> API | Search query input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | /api/v3/blog/search | mitigate | Use GORM's parameterized queries to prevent SQL injection in the search term |
| T-03-07 | Denial of Service | Search API | mitigate | Implement pagination and a maximum query length limit |
</threat_model>

<verification>
- Search returns relevant results even with slight typos.
- Related posts are accurately determined by tags/categories.
- Search performance is optimal due to GIN indices.
</verification>

<success_criteria>
- Full-text search and related posts are functional and performant.
</success_criteria>

<output>
After completion, create `.planning/phases/03-blog-core-lifecycle/03-04-SUMMARY.md`
</output>
