---
phase: 03-blog-core-lifecycle
plan: 06
type: execute
wave: 5
depends_on: [02]
files_modified:
  - backend/internal/modules/blog/service.go
  - backend/internal/modules/blog/handler.go
autonomous: true
requirements: [BLOG-11]
---

<objective>
Implement automatic XML sitemap generation for published blog content.

Purpose: Improve search engine indexability by providing a machine-readable list of all public posts.
Output: `/sitemap.xml` API endpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-blog-core-lifecycle/03-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement XML Sitemap Generator</name>
  <files>backend/internal/modules/blog/service.go</files>
  <action>
    Implement the sitemap generation logic:
    1. Fetch all posts with `status = 'Published'`.
    2. Construct an XML document following the sitemap.org protocol.
    3. Include `<loc>` (absolute URL), `<lastmod>` (published_at), and `<changefreq>` (monthly/weekly).
    4. Ensure the XML is properly escaped and formatted.
  </action>
  <verify>
    <automated>
      # Call service and verify XML output contains published post URLs
      # (Internal test or via API)
    </automated>
  </verify>
  <done>Sitemap generator produces valid XML for all published content</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sitemap API Endpoint</name>
  <files>backend/internal/modules/blog/handler.go</files>
  <action>
    Expose the sitemap via a public endpoint:
    - GET `/sitemap.xml` : Calls the sitemap generator and returns the response with `Content-Type: application/xml`.
    - Ensure this endpoint is public (no auth required).
  </action>
  <verify>
    <automated>
      curl -I http://localhost:8080/sitemap.xml | grep "application/xml" && \
      curl http://localhost:8080/sitemap.xml | grep "url"
    </automated>
  </verify>
  <done>The sitemap is publicly accessible at /sitemap.xml</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public -> API | Unauthenticated access to sitemap |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Denial of Service | /sitemap.xml | mitigate | Cache the sitemap XML in Redis and regenerate it only on post status changes |
</threat_model>

<verification>
- The endpoint `/sitemap.xml` returns valid XML.
- Only `Published` posts are included.
- Response header is correctly set to `application/xml`.
</verification>

<success_criteria>
- Automatic XML sitemap is generated and served.
</success_criteria>

<output>
After completion, create `.planning/phases/03-blog-core-lifecycle/03-06-SUMMARY.md`
</output>
